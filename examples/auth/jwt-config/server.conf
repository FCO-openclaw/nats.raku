# Basic server configuration
port: 4222
debug: true
trace: false
jetstream: enabled

# JWT-based authentication setup
# For testing, we'll start with a simpler configuration
# and expand to full JWT when we have the proper tools
authorization {
  token: "jwt_test_token"
}